
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mytrainerapp">

    <!-- Дозвіл на доступ до Інтернету -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="${applicationName}"
        android:label="MyTrainer"
        android:icon="@mipmap/launcher_icon">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Тема, яка застосовується до Activity під час запуску -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Не видаляти: використовується Flutter для генерації плагінів -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Запити для обробки тексту -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            /plain" />
        </intent>
    </queries>
</manifest>
