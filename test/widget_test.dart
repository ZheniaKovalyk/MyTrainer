import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // This is a basic smoke test to verify the test setup works
    expect(true, isTrue);
  });
}
